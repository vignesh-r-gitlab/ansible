- name: demo on conditions
  hosts: local 
  connection: local
  vars:
    IS_LIVE: fasle
  tasks:
  - name: check live or not 
    ansible.builtin.debug:
      msg: "yes, it is live"
    when: IS_LIVE
